services:
  db:
    image: postgres:15
    environment:
      POSTGRES_DB: "trepa"
      POSTGRES_USER: "postgres"
      POSTGRES_PASSWORD: "postgres"
    ports:
      - "5433:5432"
      # command: ["postgres", "-c", "log_statement=all"]